[package]
name = "rustywin"
version = "0.0.1"
authors = ["Gian-Carlo Pascutto <gcp@sjeng.org>"]
readme = "README.md"
license = "MPL-2.0"

[dependencies]
clippy = { version = "*", optional = true }
log = "0.4"
env_logger = "0.5"
nix = "0.10"
libc = "0.2"
itertools = "0.7"

[features]
default = []
